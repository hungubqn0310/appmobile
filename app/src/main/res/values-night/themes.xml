<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherForecastApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Dòng này dùng để thay font toàn app -->
        <item name="android:fontFamily">@font/overpass_light</item>

        <!-- Nếu muốn áp dụng cho cả TextView của MaterialComponents -->
        <item name="fontFamily">@font/overpass_light</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WeatherForecastApp" parent="Base.Theme.WeatherForecastApp" />
</resources>